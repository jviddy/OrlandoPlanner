# npm 11's peer-set resolver trips over the Nuxt 4 + Vite dep graph
# ("Cannot read properties of null (reading 'edgesOut')"). legacy-peer-deps
# sidesteps that code path. Cloudflare Pages' build image respects this file.
legacy-peer-deps=true
