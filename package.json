{
  "name": "orlando-planner",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@11.4.2",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "typecheck": "nuxt typecheck",
    "cf:preview": "npm run build && wrangler pages dev dist",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=orlando-planner"
  },
  "dependencies": {
    "@pinia/nuxt": "^1.0.2",
    "pinia": "^4.0.3",
    "pinia-plugin-persistedstate": "^4.7.1"
  },
  "devDependencies": {
    "nuxt": "^4.5.2",
    "typescript": "^5.9.2",
    "vue-tsc": "^2.2.0",
    "wrangler": "^4.26.0"
  }
}
