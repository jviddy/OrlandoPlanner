{
  "name": "orlando-planner",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@11.4.2",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "test": "vitest run",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "typecheck": "nuxt typecheck",
    "db:migrate:local": "wrangler d1 migrations apply orlando-planner-preview --local",
    "db:migrate:preview": "wrangler d1 migrations apply orlando-planner-preview --remote",
    "db:migrate:production": "wrangler d1 migrations apply orlando-planner-production --remote --env production",
    "db:list:preview": "wrangler d1 migrations list orlando-planner-preview --remote",
    "db:list:production": "wrangler d1 migrations list orlando-planner-production --remote --env production",
    "db:verify": "sh scripts/verify-d1-migrations.sh",
    "cf:preview": "npm run build && wrangler pages dev dist",
    "deploy": "echo 'Cloudflare Pages deploys automatically on git push. Run: git push origin main'",
    "deploy:legacy": "npm run build && wrangler pages deploy dist --project-name=orlando-planner"
  },
  "dependencies": {
    "@pinia/nuxt": "^1.0.2",
    "html-to-image": "^1.11.13",
    "pinia": "^4.0.3",
    "pinia-plugin-persistedstate": "^4.7.1"
  },
  "devDependencies": {
    "nuxt": "^4.5.2",
    "typescript": "^5.9.2",
    "vitest": "^3.2.7",
    "vue-tsc": "^2.2.0",
    "wrangler": "^4.26.0"
  }
}
